name: Security Scan

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  schedule:
    # Run weekly on Monday at 9 AM UTC
    - cron: '0 9 * * 1'

jobs:
  security-scan:
    name: Security Vulnerability Scan
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run npm audit
        run: |
          echo "🔍 Running npm audit..."
          npm audit --audit-level=moderate || true
          npm audit --json > audit-report.json || true
      
      - name: Check for exposed environment variables
        run: |
          echo "🔍 Checking for exposed environment variables..."
          
          # Ensure .env is in .gitignore
          if ! grep -q "\.env$" .gitignore 2>/dev/null && ! grep -q "^\.env" .gitignore 2>/dev/null; then
            echo "⚠️  WARNING: .env might not be in .gitignore"
          fi
          
          # Check if .env is tracked
          if git ls-files | grep -q "^\.env$"; then
            echo "❌ ERROR: .env file is tracked in git!"
            exit 1
          fi
          
          echo "✅ Environment files properly ignored"
          
          # Note: Hardcoded secrets are checked in PR tests workflow
          # This scan focuses on dependency vulnerabilities
      
      - name: Security Summary
        if: always()
        run: |
          echo "## Security Scan Results"
          echo ""
          echo "✅ Security scan completed"
          echo ""
          echo "**Checks Performed:**"
          echo "- npm audit for dependency vulnerabilities"
          echo "- Environment variable exposure check"
          echo ""
          echo "**Note:** Hardcoded secrets are checked in PR tests workflow"
          echo ""
          echo "**Next Steps:**"
          echo "- Review any warnings above"
          echo "- Update dependencies if vulnerabilities found"
          echo "- Ensure no secrets are committed to repository"
