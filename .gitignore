# Environment variables
.env
.env.local
.env.production

# Node modules (if you add any build tools)
node_modules/
# Note: package-lock.json and yarn.lock should be committed for production
# to ensure consistent dependency versions across environments

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs (if you add build tools)
dist/
build/
*.min.js
*.min.css

# IMPORTANT: Source files should NEVER contain real credentials
# They should always have placeholders (VITE_*)
# - Local dev: Use `npm run dev` (dev server does on-the-fly replacement)
# - Netlify: Build script modifies source files during deployment only
# - Always restore placeholders before committing!

# Temporary files
tmp/
temp/
.cache/

# OS files
Thumbs.db
.DS_Store
